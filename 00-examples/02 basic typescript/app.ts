/**
 * Imports the `displaySomeData` function from the "./app_module" module.
 */
import { displaySomeData } from "./app_module";

/**
 * Calls the `displaySomeData` function with the provided message and logs the result.
 * @param {string} message - The message to be displayed.
 */
console.log(displaySomeData("I just got changed in TypeScript and then compiled."));
